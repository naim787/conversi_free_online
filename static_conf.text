import adapter from '@sveltejs/adapter-static';

export default {
  kit: {
    adapter: adapter({
      pages: 'build',       // output folder
      assets: 'build',      // asset folder
      fallback: null,       // bisa diganti '200.html' untuk SPA fallback
      precompress: false
    }),
    prerender: {
      default: true         // prerender semua route secara default
    }
  }
};

